var smartListing = document.getElementById('<%= name.to_s %>');
smartListing.dispatchEvent(new CustomEvent('smart-listing:destroy', {
  detail: {
    id: <%= id.to_json.html_safe %>,
    destroyed: <%= object.destroyed? %>
  }
}));

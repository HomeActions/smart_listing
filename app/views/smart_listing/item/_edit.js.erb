var smartListing = document.getElementById('<%= name %>');
smartListing.dispatchEvent(new CustomEvent('smart-listing:edit', {
  detail: {
    id: <%= id.to_json.html_safe %>,
    content: "<%= escape_javascript(render(:partial => part, :locals => {object_key => object})) %>"
  }
}));
